BackslashUi.TestsController = Ember.ObjectController.extend({
/*    queryParams: ['session_id'],
    sessionId: null,

    filteredTests: function() {
        asdasdasd;
        var session_id = this.get('sessionId');
        var tests = this.get('model');

        if (session_id) {
            return tests.filterBy('session_id', session_id);
        } else {
            return tests;
        }
    }.property('sessionId', 'model')
    */
});